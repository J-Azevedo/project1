FUNC void displayvalues(void) {
  int idx;

  exec("log > truedata21.log");
	//exec(c);
  for (idx = 0; idx < 500; idx++) {
    printf ("%d x %f y %f z %f\n", idx, data[idx].x, data[idx].y,data[idx].z);
  }
  exec("log off");
}